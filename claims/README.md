# Affirmations

Les fichiers `*.json` de ce dossier suivent le contrat décrit dans `schemas/claim.schema.json` lorsqu'il est présent et sont contrôlés par `scripts/validate_claims.py`.

Une affirmation n'est jamais promue sur la seule autorité d'un modèle de langage.
